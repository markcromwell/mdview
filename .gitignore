# Node / JS
node_modules/
npm-debug.log*

# Build/QA output (regenerate with `npm run samples`)
# (samples/ is committed as demo output; uncomment to stop tracking it)
# samples/

# Forge runtime artifacts
.forge-access.lock
.forge-worker.lock

# OS
.DS_Store
Thumbs.db
